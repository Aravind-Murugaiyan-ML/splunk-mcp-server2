{
  "name": "splunk-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for retrieving data from Splunk",
  "main": "server.js",
  "bin": {
    "splunk-mcp-server": "./server.js"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "ts-node server.ts",
    "start:node": "node server.js",
    "prestart:node": "npm run build",
    "sse-client": "ts-node client_sse.ts",
    "stdio-client": "ts-node client_stdio.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/express": "^5.0.3",
    "@types/node": "^20.0.0",
    "@types/xml2js": "^0.4.14",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^5.1.0",
    "xml2js": "^0.6.2",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
